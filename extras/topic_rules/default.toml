# nostos topic curation rules - default set
#
# Curated baseline that denoises the most common GitHub topic sprawl
# encountered when --auto-tags imports topics from offensive- and
# defensive-security repos. Conservative on purpose: no language /
# distro tags are dropped, and only obvious junk is denied. The
# alias map collapses the most common synonym fragmentation so that
# `nostos list --tag pentest` etc. resolve against one canonical
# form regardless of how a given upstream maintainer chose to spell
# it.
#
# Apply with:
#   nostos topics import extras/topic_rules/default.toml
#       (default --merge: unions with your existing rules)
#
#   nostos topics import extras/topic_rules/default.toml --replace
#       (overwrites your local rules entirely)
#
# Tweak after import via `nostos topics deny / allow / alias / unalias`.
# This file is a starting point, not a mandate.

deny = [
  # Event markers with no fleet-management value:
  "hacktoberfest",
  # Placeholder topics that occasionally leak from upstreams:
  "foo",
  # Repo-name echoes (a maintainer tagged the repo with its own name) seen
  # in real fleets. Add more with `nostos topics deny <name>` as you spot them.
  "paybag",
  "lazyscript",
  # Generic words too broad to filter on (added 1.4.6):
  "tool", "list", "lib", "files", "extract", "urls", "endpoints",
  # UI / extension targets unrelated to the security work the repo does:
  "build-tool", "gui", "customtkinter", "chrome-extension",
  "firefox-extension", "chrome-headless", "chromium", "grunt-plugins",
  # Implementation-detail noise:
  "multithreading", "asynchronous", "thread", "pipeline", "socket", "pcre",
  # Operational adjectives without category value:
  "automated", "realtime", "monitor", "filtering",
]

[alias]
# --- Penetration testing ---
"penetration-testing"            = "pentest"
"penetration-testing-tools"      = "pentest"
"pentesting"                     = "pentest"
"pentesting-tools"               = "pentest"
"pentest-tool"                   = "pentest"
"pentest-tools"                  = "pentest"

# --- Red-teaming ---
"red-teaming"                    = "redteam"
"red-team"                       = "redteam"
"red-teaming-tools"              = "redteam"
"redteam-tools"                  = "redteam"
"readteaming"                    = "redteam"

# --- Hacking ---
"hacking-tool"                   = "hacking"
"hacking-tools"                  = "hacking"

# --- Payloads ---
"payloads"                       = "payload"
"payload-generator"              = "payload"
"payload-generators"             = "payload"

# --- Recon / OSINT noise ---
"dork"                           = "dorking"
"dorks"                          = "dorking"
"google-dorks"                   = "dorking"
"information-gathering"          = "recon"
"reconnaissance"                 = "recon"
"osint-tool"                     = "osint"
"osint-tools"                    = "osint"

# --- Bug bounty ---
"bounty"                         = "bugbounty"
"bug-bounty"                     = "bugbounty"
"bug-hunting"                    = "bugbounty"

# --- Offensive security branding (canonical: offsec - shorter, idiomatic) ---
"offensive-security"             = "offsec"
"osep-prep"                      = "osep"

# --- Reverse-shells / shells ---
"reverse-shells"                 = "reverse-shell"
"shell-script"                   = "shell"

# --- Vulnerability sprawl (collapse the family to one umbrella) ---
"vulnerability-management"       = "vulnerability"
"vulnerability-detection"        = "vulnerability"
"vulnerability-assessment"       = "vulnerability"
"vulnerability-scanners"         = "vulnerability"
"vulnerability-scanner"          = "vulnerability"

# --- XSS family ---
"xss-exploitation"               = "xss"
"xss-vulnerability"              = "xss"
"cross-site-scripting"           = "xss"

# --- Greenbone / OpenVAS family (Greenbone is the rebranded OpenVAS suite;
#     GVM = Greenbone Vulnerability Management; CE is the community edition) ---
"greenbone-community-edition"        = "greenbone"
"greenbone-vulnerability-management" = "greenbone"
"gvm"                                = "greenbone"
"openvas-scanner"                    = "openvas"

# --- Metasploit family ---
"metasploit-payloads"            = "metasploit"
"launch-metasploit"              = "metasploit"

# --- Brute-force ---
"brute-force"                    = "bruteforce"
"brute-force-attacks"            = "bruteforce"

# --- Web testing ---
"web-penetration-testing"        = "websec"
"web-application"                = "websec"

# --- Linux noise ---
"linux-hacking-tools"            = "linux"

# --- Typos seen in the wild ---
"sheatsheet"                     = "cheatsheet"
"cybersecurty"                   = "cybersecurity"
"cyber-security"                 = "cybersecurity"
"offensivesecurity"              = "offsec"

# --- Language variants (light touch) ---
"python3"                        = "python"
"py3"                            = "python"
"py"                             = "python"

# --- Distro variants (light touch) ---
"kali-linux"                     = "kali"
"termux-hacking"                 = "termux"
"termux-tool"                    = "termux"
"termux-tools"                   = "termux"

# --- Misc collapses ---
"penetration-tester"             = "pentester"
"web-security"                   = "websec"
"web-application-security"       = "websec"
"network-security"               = "netsec"

# --- Pentest variants (added in 1.4.5) ---
"penetration-test"               = "pentest"
"pentesting-guides"              = "pentest"
"linux-pentesting"               = "pentest"

# --- Hacking variants (added in 1.4.5) ---
"web-hacking"                    = "hacking"
"ethical-hacking"                = "hacking"
"ethical-hacking-tools"          = "hacking"

# --- Security umbrella (added in 1.4.5).
#     Existing convention: cybersecurity is canonical (cyber-security and
#     cybersecurty already alias to it). Adding `security -> cybersecurity`
#     completes the family - 'security' alone is too broad to filter on. ---
"security"                       = "cybersecurity"

# --- Port scan variants (added in 1.4.5) ---
"port-scanner"                   = "port-scan"
"portscanner"                    = "port-scan"
"port-enumeration"               = "port-scan"
"scan-ports"                     = "port-scan"

# --- XSS flavor variants (added in 1.4.5) ---
"xss-scanner"                    = "xss"
"xss-detection"                  = "xss"
"xss-exploit"                    = "xss"
"xss-python"                     = "xss"
"xss-bruteforce"                 = "xss"
"xsstrike"                       = "xss"

# --- WAF variants (added in 1.4.5) ---
"waffit"                         = "waf"
"web-application-firewall"       = "waf"
"waf-detection"                  = "waf"

# --- CORS variants (added in 1.4.5) ---
"cors-misconfiguration-scanner"  = "cors"
"cors-misconfigurations"         = "cors"
"cors-policy"                    = "cors"
"cors-scanner"                   = "cors"

# --- CSRF variants (added in 1.4.5) ---
"csrf-scanner"                   = "csrf"
"xsrf"                           = "csrf"

# --- Subdomain takeover variants (added in 1.4.5) ---
"subdomain-takeovers"            = "subdomain-takeover"
"takeover-subdomain"             = "subdomain-takeover"
"hostile-subdomain-takeover"     = "subdomain-takeover"

# --- Virtual host variants (added in 1.4.5) ---
"vhosts"                         = "vhost"
"virtual-host"                   = "vhost"
"virtual-hosts"                  = "vhost"

# --- DNS variants (added in 1.4.5) ---
"dns-bruteforcer"                = "dns"
"dns-resolution"                 = "dns"
"dns-resolver"                   = "dns"
"subdomain-bruteforcing"         = "subdomain"

# --- Vulnerability variants (added in 1.4.5) ---
"vulnerabilities"                = "vulnerability"
"security-vulnerability"         = "vulnerability"

# --- MITRE ATT&CK family (added in 1.4.5) ---
"mitre-attack"                   = "mitre"
"mitre-corporation"              = "mitre"

# --- Singletons collapsing to existing canonical forms (added in 1.4.5) ---
"wordlists"                      = "wordlist"
"server-side-request-forgery"    = "ssrf"
"open-redirections"              = "open-redirect"
"secrets-management"             = "secret-management"
"subdomains"                     = "subdomain"

# --- 1.4.6 long-tail collapses ---
# Subdomain takeover - more variants to fold into the existing canonical
"takeover"                       = "subdomain-takeover"
"hostile"                        = "subdomain-takeover"

# Parameter family
"parameter-finder"               = "parameter"
"urls-parameters"                = "parameter"

# Secret family - keep `secret` as canonical (singular, matches existing convention)
"secrets"                        = "secret"

# SBOM family - collapse generator/tool variants to the umbrella term
"sbom-generator"                 = "sbom"
"sbom-tool"                      = "sbom"

# Breach family
"breach-checker"                 = "breach"
"data-breach"                    = "breach"

# Language typo
"c-plus-plus"                    = "cpp"
